import { Input } from "../";

interface ILoginFormProps {
    parentWidth: number;
}

export function LoginForm({ parentWidth }: ILoginFormProps) {
    return (
        <div>
            <Input />
        </div>
    );
}
